export enum Collection {
  USERS = 'users',
  ADMINS = 'admins',
  RESTAURANTS = 'restaurants',
  ORDERS = 'orders',
  RECIPES = 'recipes',
  PAYMENTS = 'payments',
  MENUS = 'menu'
}

export enum RequestMethods {
  POST = "POST",
  PUT = "PUT",
  DELETE = "DELETE",
  GET = "GET",
  PATCH = "PATCH"
}
export enum Collection {
  USERS = 'users',
  ADMINS = 'admins',
  RESTAURANTS = 'restaurants',
  ORDERS = 'orders',
  RECIPES = 'recipes',
  PAYMENTS = 'payments'
}

export enum RequestMethods {
  POST = "POST",
  PUT = "PUT",
  DELETE = "DELETE",
  GET = "GET",
  PATCH = "PATCH"
}